# ✅ 완료 보고서 — 채팅 초기화·개편 v0.18.0~v0.19.3 (Claude Code, 2026.08.07)

- 작업 브랜치: `claude/chat-content-reset-e0e661` (워크트리 세션)
- PR: https://github.com/kuroicode-beep/tether/pull/2
- 배포: https://tether-d1dab.web.app (전 버전 라이브 반영 완료)

## 01. 작업 요약

- 목표: 채팅 데이터 전체 초기화 + 순서 꼬임·속도 문제 해결 + 입력 편의(한글 IME·입력 중 표시·알림 정리)
- 결과: 완료 (7개 릴리즈, 커밋 7개)

## 02. 릴리즈별 내용

| 버전 | 커밋 | 내용 |
|---|---|---|
| v0.18.0 | 1c42a38 | 메시지 시각 serverTimestamp 통일(기기 시계 어긋남 해결), 오늘 대화만 우선 로딩 + "이전 내용 보기" 버튼(30건씩) |
| v0.18.1 | f106792 | 대량 삭제 후 IndexedDB 유령 캐시 1회성 퍼지(플래그 `tether:fscache-purged:2026-08-07`), findIndex O(n²) 제거, MessageBubble 메모이즈 |
| v0.18.2 | 48ab9ee | 채팅 입력창 `lang="ko"` 힌트 (한계 있음 → v0.19.2로 대체) |
| v0.19.0 | 45e532b | 상대방 "입력 중" 표시 — `couples/{id}/typing/{uid}` 하트비트(2.5초 스로틀, TTL 6초), firestore.rules typing 규칙 배포 |
| v0.19.1 | f0eddc6 | 미읽음 배지 리스너 한도(채팅 100·일기 50·콘텐츠 lastRead 이후 50), 화면 14개 React.lazy 분할(초기 JS 1,152KB→178KB+벤더), manualChunks |
| v0.19.2 | 0bceea9 | 사이드카 v0.4.0 `/ime/hangul` — 채팅 입력창 포커스 시 WM_IME_CONTROL로 한글 자동 전환(실PC 검증 완료) |
| v0.19.3 | 38f3791 | 채팅 화면 보는 중엔 메시지 알림(토스트·소리) 끔 — FCM 포그라운드·SW 사운드 경로 양쪽 |

## 03. 데이터 작업 (비가역, 코드 외)

- 2026-08-07 채팅 전체 삭제(Admin SDK): 메시지 12,141건, 파일 인덱스 16건, Storage 이미지 39·파일 21
- **백업(유일본)**: `C:\Users\kuroi\Documents\tether_chat_backup_20260807.json` — 삭제 전 전체 메시지 JSON
- 릴레이소설 서재(`relayNovels`)는 별도 컬렉션이라 보존됨

## 04. 검증 결과

- 각 릴리즈마다 `tsc` 타입체크 + `vite build` 통과 후 배포
- 사이드카 `/ime/hangul` 엔드포인트: 실행 중 인스턴스 v0.4.0 재시작 후 실제 한글 전환(`OK:1`) 확인
- Firestore rules(typing) 배포 완료, 복합 인덱스 불필요(단일 필드 range+orderBy)

## 05. 특이점 / 결정사항

- 대량 삭제 후 클라이언트 유령 캐시 → 앱 전역 저하: 원인은 미읽음 배지의 **무제한 컬렉션 구독**이 전체 히스토리를 캐시에 쌓던 것. 한도 적용으로 재발 차단.
- 웹은 OS IME를 제어할 수 없어 로컬 사이드카에 위임(전경 창이 Tether일 때만 동작).
- 향후 대량 삭제 시 캐시 퍼지 플래그 키를 새 날짜로 갱신해야 재퍼지됨.
- 사이드카 코드 수정 시 메인 저장소 `sidecar/`에 복사 + 재시작(포트 48620)해야 실반영.

## 06. 남은 작업

- [ ] PR #2 병합 및 워크트리 정리 (이 마무리 루틴에서 진행)
- [ ] 새 MS IME에서 WM_IME_CONTROL 미동작 기기 발견 시: 이전 버전 IME 호환성 옵션 안내 (담당: 소장님 확인 후)

## 07. Git 커밋

- 1c42a38 → 38f3791 (7개), 배포: 전부 완료
